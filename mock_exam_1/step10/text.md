### Troubleshoot and fix the failing pod `broken-pod` in the `default` namespace. The pod is currently in an error state. Once fixed, ensure the pod is running successfully.

**Hint**: Use `kubectl describe pod broken-pod` or `kubectl get events` to investigate the issue.

