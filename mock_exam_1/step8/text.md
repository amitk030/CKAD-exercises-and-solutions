### Create a pod named `multi-container-pod` with two containers:
1. Container `nginx` with image `nginx:1.18.0` exposing port `80`
2. Container `redis` with image `redis:6-alpine` exposing port `6379`

Both containers should be in the `default` namespace.

