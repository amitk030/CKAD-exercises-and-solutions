### Create a deployment named `api-deployment` with image `nginx:1.19.8` and `3` replicas in the `default` namespace. Expose port `80` on the containers. Then update the deployment to use image `nginx:1.20.0`.

