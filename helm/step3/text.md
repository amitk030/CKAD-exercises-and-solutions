### Install the `wordpress` chart from the bitnami repo in the `default` namespace.

<details><summary>Solution</summary>
  <p>

  ```bash
  helm install my-wordpress bitnami/wordpress
  ```
  </p>
</details>